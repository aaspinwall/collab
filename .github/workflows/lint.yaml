name: ESLint

on: pull_request

jobs:
  lint_pull_request:
    runs_on: ubuntu_latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-verion: 12
      - run: npm ci
      - run: npm lint
